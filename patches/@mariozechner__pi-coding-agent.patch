diff --git a/dist/core/compaction/compaction.d.ts b/dist/core/compaction/compaction.d.ts
index ecfcfbdda13eb050f24ad0ab9ffb24f451f433e5..8281a97bac8590b322b9c03ac32997b1b796dc8e 100644
--- a/dist/core/compaction/compaction.d.ts
+++ b/dist/core/compaction/compaction.d.ts
@@ -91,7 +91,7 @@ export declare function findCutPoint(entries: SessionEntry[], startIndex: number
  * Generate a summary of the conversation using the LLM.
  * If previousSummary is provided, uses the update prompt to merge.
  */
-export declare function generateSummary(currentMessages: AgentMessage[], model: Model<any>, reserveTokens: number, apiKey: string, signal?: AbortSignal, customInstructions?: string, previousSummary?: string): Promise<string>;
+export declare function generateSummary(currentMessages: AgentMessage[], model: Model<any>, reserveTokens: number, apiKey: string, signal?: AbortSignal, customInstructions?: string, previousSummary?: string, options?: { onPayload?: (payload: unknown) => void }): Promise<string>;
 export interface CompactionPreparation {
     /** UUID of first entry to keep */
     firstKeptEntryId: string;
diff --git a/dist/core/compaction/compaction.js b/dist/core/compaction/compaction.js
index 9a20125dccea0094e70ff3cad375d783069f0a6f..ad747d447ff9202429c9b317b14f94014d0d87ee 100644
--- a/dist/core/compaction/compaction.js
+++ b/dist/core/compaction/compaction.js
@@ -420,7 +420,7 @@ Keep each section concise. Preserve exact file paths, function names, and error
  * Generate a summary of the conversation using the LLM.
  * If previousSummary is provided, uses the update prompt to merge.
  */
-export async function generateSummary(currentMessages, model, reserveTokens, apiKey, signal, customInstructions, previousSummary) {
+export async function generateSummary(currentMessages, model, reserveTokens, apiKey, signal, customInstructions, previousSummary, options) {
     const maxTokens = Math.floor(0.8 * reserveTokens);
     // Use update prompt if we have a previous summary, otherwise initial prompt
     let basePrompt = previousSummary ? UPDATE_SUMMARIZATION_PROMPT : SUMMARIZATION_PROMPT;
@@ -445,8 +445,8 @@ export async function generateSummary(currentMessages, model, reserveTokens, api
         },
     ];
     const completionOptions = model.reasoning
-        ? { maxTokens, signal, apiKey, reasoning: "high" }
-        : { maxTokens, signal, apiKey };
+        ? { maxTokens, signal, apiKey, reasoning: "high", onPayload: options?.onPayload }
+        : { maxTokens, signal, apiKey, onPayload: options?.onPayload };
     const response = await completeSimple(model, { systemPrompt: SUMMARIZATION_SYSTEM_PROMPT, messages: summarizationMessages }, completionOptions);
     if (response.stopReason === "error") {
         throw new Error(`Summarization failed: ${response.errorMessage || "Unknown error"}`);
